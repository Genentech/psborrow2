# Cut Off Object

Cut Off Object

## Slots

- `cut_off_fun`:

  A function that takes a `data.frame` with columns of enrollment time,
  survival time and outcome. The function returns a modified
  `data.frame` after applied the cut-off rule.
